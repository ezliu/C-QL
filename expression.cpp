//expression.cpp
#include "expression.h"

#include <sstream>
#include <iostream>

CreateExpression::CreateExpression(const std::string &type)
{
	//this->type = type;
	std::cout << "CreateExpression()" << std::endl;
}

void CreateExpression::evaluate(EvaluationContext &context)
{
}
